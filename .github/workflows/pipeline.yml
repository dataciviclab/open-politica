# Pipeline — open-politica
#
# Post-merge + refresh. Usa pipeline-reusable.yml dell'org.
# L'ordinamento (extract → datasets → ponte → compose) è gestito da
# scripts/run_pipeline.sh, che la reusable chiama come run-command.

name: Pipeline

on:
  pull_request:
    types: [closed]
    paths:
      - "datasets/**"
      - "compose/**"
      - "scripts/**"
  schedule:
    - cron: '0 6 1 * *'
  workflow_dispatch:

concurrency:
  group: open-politica-pipeline-${{ github.event.pull_request.number || github.ref || 'schedule' }}
  cancel-in-progress: false

jobs:
  pipeline:
    if: github.event.pull_request.merged == true || github.event_name != 'pull_request'
    uses: dataciviclab/.github/.github/workflows/pipeline-reusable.yml@main
    with:
      repo-slug: "open-politica"
      run-command: "TOOLKIT_ALLOW_SCRIPT_SOURCE=1 make run-all"
      sync-pattern: "per-dataset"
    secrets: inherit
